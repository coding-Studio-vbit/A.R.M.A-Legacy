import React from "react";
import { Button } from "react-bootstrap";

const Test = () => {
  const ff = () => {
    console.log("fdf");
  };
  return (
    <div>
      <button onClick={ff}>fdsf</button>
    </div>
  );
};

export default Test;
